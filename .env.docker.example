# ==================================================
# CONFIGURAÇÃO DOCKER - USO LOCAL
# ==================================================
# Este arquivo contém configurações para rodar a
# aplicação com Docker localmente (desenvolvimento/homologação)
#
# Para produção em servidor, ajuste:
# - APP_URL para seu domínio
# - Senhas mais fortes
# - Configure SSL/HTTPS
# ==================================================

# Application
APP_NAME="Sistema de Tickets"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000
APP_PORT=8000
APP_TIMEZONE=America/Sao_Paulo
APP_LOCALE=pt_BR
APP_FALLBACK_LOCALE=pt_BR
APP_FAKER_LOCALE=pt_BR

# Database
DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=tickets
DB_USERNAME=tickets
DB_PASSWORD=tickets

# Cache & Sessions (Development: usa file, Production: usa redis)
CACHE_DRIVER=file
SESSION_DRIVER=file
SESSION_LIFETIME=120

# Queue (Development: sync = imediato, Production: redis com workers)
QUEUE_CONNECTION=sync

# Redis (comentado para desenvolvimento, descomentar para produção)
# REDIS_HOST=redis
# REDIS_PASSWORD=null
# REDIS_PORT=6379

# Mail
MAIL_MAILER=smtp
MAIL_SCHEME=
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=imogerence@gmail.com
MAIL_PASSWORD="puul uajl tojy daoq"
MAIL_FROM_ADDRESS="imogerence@gmail.com"
MAIL_FROM_NAME="${APP_NAME}"

# Docker
RUN_SEED=true

# Logging
LOG_CHANNEL=stack
LOG_STACK=single
LOG_LEVEL=error
